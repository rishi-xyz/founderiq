{
  "name": "founderiq",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "dev:api" : "turbo dev --filter=api-gateway",
    "build": "turbo build",
    "build:api": "turbo build --filter=api-gateway",
    "test": "turbo test",
    "test:api": "turbo test --filter=api-gateway",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{js,jsx,ts,tsx,json,css,md}\""
  },
  "devDependencies": {
    "turbo": "^2.4.0",
    "prettier": "^3.5.0"
  },
  "workspaces": ["apps/*", "packages/*"],
  "packageManager": "bun@1.3.10",
  "engines": {
    "node": ">=18"
  }
}
