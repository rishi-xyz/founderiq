{
  "name": "founderiq",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{js,jsx,ts,tsx,json,css,md}\""
  },
  "devDependencies": {
    "turbo": "^2.4.0",
    "prettier": "^3.5.0"
  },
  "workspaces": ["apps/*", "packages/*"],
  "packageManager": "bun@1.3.10",
  "engines": {
    "node": ">=18"
  }
}
