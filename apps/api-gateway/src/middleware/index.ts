/** Barrel export: `ApiError`, `AppError`, `securityHeaders`. */
export * from "./errors"
/** Auth guard: `isAuthenticated` — verifies access_token cookie on protected routes. */
export * from "./auth"
